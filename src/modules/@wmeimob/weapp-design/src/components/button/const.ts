
export enum MMButtonType {
    Default,
    Hollow,
    White
}

export enum MMButtonColor {
    Default,
    Red
}

export enum MMButtonSize {
    Default,
    Tiny,
    Small
}

export enum MMButtonRadius {
    Default,
    None,
    Large
}
