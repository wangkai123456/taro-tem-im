
export enum MMButtonType {
    Default,
    Hollow,
    White
}

export enum MMButtonState {
    Default,
    Success,
    Warning,
    Danger
}

export enum MMButtonSize {
    Default,
    Tiny,
    Small
}

export enum MMButtonRadius {
    Default,
    None,
    Large
}
